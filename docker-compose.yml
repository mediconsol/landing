services:
  landing:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: landing
    restart: unless-stopped
    depends_on:
      - landing-api
    networks:
      - proxy-network

  landing-api:
    build:
      context: ./api
      dockerfile: Dockerfile
    container_name: landing-api
    restart: unless-stopped
    volumes:
      - landing-data:/data
    networks:
      - proxy-network

volumes:
  landing-data:

networks:
  proxy-network:
    external: true
